# build output
dist/
.astro/

# dependencies
node_modules/

# environment variables — NUNCA committear (token de shadcnblocks vive aquí)
.env
.env.*

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# playwright
test-results/
playwright-report/

# macOS / editor
.DS_Store

nginx.generated.conf

# Prisma: cliente generado (binarios de motor incluidos) — se regenera
# fresco en cada build vía `prisma generate`, nunca se commitea
backend/src/generated/
backend/dist/
